# My Mail - Environment Configuration for Shared Hosting

# Server Configuration
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://mail.ventrucci.online

# Database Configuration
# For your shared hosting, use MariaDB (MySQL compatible)
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=grooveno_mail
DB_PASSWORD=Am53hhjMMzmaf3F2Xqhj
DB_NAME=grooveno_mail

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Mail Server Configuration (Your shared hosting mail server)
MAIL_IMAP_SERVER=mail.ventrucci.online
MAIL_IMAP_PORT=993
MAIL_SMTP_SERVER=mail.ventrucci.online
MAIL_SMTP_PORT=587
MAIL_USERNAME=your_mail_username
MAIL_PASSWORD=your_mail_password
MAIL_FROM_EMAIL=noreply@ventrucci.online

# CORS Configuration
CORS_ORIGIN=https://mail.ventrucci.online

# Encryption Key for storing passwords
ENCRYPTION_KEY=your-encryption-key-change-this-in-production

# Optional: Email verification settings
EMAIL_VERIFICATION_ENABLED=true
EMAIL_VERIFICATION_EXPIRY_HOURS=24

# Optional: Password reset settings
PASSWORD_RESET_ENABLED=true
PASSWORD_RESET_EXPIRY_HOURS=1

# Optional: Rate limiting
RATE_LIMIT_ENABLED=false
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

